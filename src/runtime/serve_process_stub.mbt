///|
fn spawn_shell_command_pid(command : String) -> Int {
  ignore(command)
  -1
}

///|
fn poll_child_process_status(pid : Int) -> Int {
  ignore(pid)
  -1
}

///|
fn terminate_child_process_signal(pid : Int) -> Int {
  ignore(pid)
  -1
}

///|
fn sleep_millis_impl(milliseconds : Int) -> Unit {
  ignore(milliseconds)
}
