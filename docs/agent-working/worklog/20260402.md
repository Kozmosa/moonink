16:22 +0800 - Implemented first real build pipeline: load source-backed build inputs, parse frontmatter for page/article classification, render via DocFlow, fully rebuild dist/, and emit HTML files with direct-style output mapping. Added end-to-end tests and updated architecture docs.
