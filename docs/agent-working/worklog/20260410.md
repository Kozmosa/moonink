- 2026-04-10 16:xx +0800 - Replaced deprecated derive(Show) usages with derive(Debug) across core/docflow/runtime/cli, restored required Show impls for test-facing enums/errors, and normalized path-policy error formatting. Verified with moon check and moon test (50 passed).
- 2026-04-10 16:xx +0800 - Removed the remaining temporary debug-string call sites after restoring stable Show output paths, keeping moon check clean and tests at 50 passing.
- 2026-04-10 16:xx +0800 - Restored configurable pretty-route output layout: core output mapping now respects route_style, build emission uses the configured style, default fixture verifies pretty layout, and a dedicated direct-route fixture covers explicit direct output. Verified with moon check and moon test (52 passed).
- 2026-04-10 16:xx +0800 - Implemented WikiLinker MVP: indexed route-aware wikilink targets, rewrote markdown [[target]] / [[target|label]] syntax into markdown links, resolved links from build input metadata, and added pretty/direct end-to-end fixture coverage. Verified with moon check and moon test (58 passed).
- 2026-04-10 16:xx +0800 - Added the native templater MVP in two atomic steps: docflow now exposes a minimal TemplateContext with {{ variable }} interpolation and a built-in default template, and cli build now passes real site/page metadata into templating with docflow/cli coverage for title, route, site link, and content kind output. Verified with moon info && moon fmt, moon check, and moon test (60 passed).
- 2026-04-10 16:xx +0800 - Added optional template_file support for the native templater: config now parses and validates template_file, runtime loads template source relative to project_root, build reuses either the built-in template or configured template source, and a dedicated fixture covers custom template wrapping for markdown/html pages. Verified with moon info && moon fmt, moon check, and moon test (66 passed).
- 2026-04-10 16:xx +0800 - Expanded template_file coverage with runtime and CLI failure-path checks: runtime now asserts configured template contents can be loaded, CLI asserts missing template files fail with a load_template error, and dedicated missing-template fixtures keep the error path reproducible. Verified with moon info && moon fmt, moon check, and moon test (68 passed).
- 2026-04-10 16:xx +0800 - Added native templater syntax edge-case coverage in docflow: unmatched opening braces now stay literal, empty variables collapse to empty strings, adjacent variables interpolate without separators, and plain-text templates remain unchanged. Verified with moon info && moon fmt, moon check, and moon test (72 passed).
- 2026-04-10 16:xx +0800 - Verified template_file and WikiLinker compose correctly: the custom-template fixture now includes wikilinks, and CLI end-to-end coverage confirms rewritten links still appear inside the configured template wrapper alongside site/title metadata. Verified with moon info && moon fmt, moon check, and moon test (72 passed).
- 2026-04-10 16:xx +0800 - Completed template_file plus direct-route coverage: added a dedicated direct-route custom-template fixture and CLI assertions for direct-layout outputs, custom wrapper markers, and route-aware `data-url` values like `/hello.html` and `/about.html`. Verified with moon info && moon fmt, moon check, and moon test (73 passed).
- 2026-04-11 00:xx +0800 - Implemented automatic navigation / site assembly MVP in nine atomic steps: core now defines SitePage/NavigationModel/SiteAssemblyModel, cli derives page metadata and builds a page-only hierarchy, navigation renders to stable `navigation_html`, docflow templates expose that field, build injects real per-page navigation from a shared site assembly, and pretty/direct/template_file fixtures now verify active-state navigation plus article exclusion. Verified with moon info && moon fmt and moon test src/cli (22 passed), plus moon test src/docflow (26 passed).
- 2026-04-11 00:xx +0800 - Ran full repository verification after the automatic navigation/site assembly series (`moon info && moon fmt && moon check && moon test`, 85 passed) and committed the remaining formatting-only cleanup in core path-policy and docflow wikilinker files to leave the branch tidy.
- 2026-04-11 00:xx +0800 - Made the built-in default template render automatic navigation directly via `navigation_html`, and updated docflow/cli coverage so default-template pages assert the `<aside class="site-nav">` slot and rendered links are present. Verified with moon check and moon test (85 passed).
- 2026-04-12 00:xx +0800 - Added minimal navigation metadata productization: `SitePage` now carries `nav_title`/`nav_hidden`, cli derives them from frontmatter `extra`, navigation rendering uses renamed labels while excluding hidden pages, and core/cli coverage plus full verification now pass with `moon info && moon fmt && moon check && moon test` (86 passed).
- 2026-04-12 00:xx +0800 - Extended the pretty-route navigation fixture to cover frontmatter-driven `nav_title` and `nav_hidden`, updating fixture pages plus CLI end-to-end assertions so renamed labels render and hidden pages stay out of generated navigation. Verified with moon test src/cli (23 passed).
- 2026-04-12 00:xx +0800 - Extended the direct-route navigation fixture to cover frontmatter-driven `nav_title` and `nav_hidden`, updating direct-layout fixture pages plus CLI end-to-end assertions so renamed labels render and hidden pages stay out of generated navigation URLs like `/guide/index.html` and `/guide/install.html`. Verified with moon test src/cli (23 passed).
- 2026-04-12 00:xx +0800 - Added core coverage that freezes `nav_title` and `nav_hidden` as frontmatter `extra` fields, ensuring navigation metadata remains available without promoting them to first-class frontmatter keys. Verified with moon test src/core (27 passed).
- 2026-04-12 00:xx +0800 - Added CLI coverage for navigation metadata defaults: empty `nav_title` now falls back to the page title path, and only the exact string `"true"` hides a page from navigation. Verified with moon test src/cli (24 passed).
- 2026-04-12 00:xx +0800 - Finished WikiLinker MVP cleanup by extending docflow coverage for path-style targets, explicit extension targets, and html passthrough behavior while keeping markdown-only rewrites route-aware through build input metadata. Verified with moon check and moon test (90 passed).
- 2026-04-12 00:xx +0800 - Derived section-aware template context in cli build: pages and section index pages now populate `current_section_title` / `current_section_url`, breadcrumb HTML is rendered from existing page-only navigation ancestry, and pretty/direct/custom-template fixtures assert the new context without pulling articles into navigation. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Updated the built-in default template to render section context and breadcrumb output directly, and expanded docflow/cli coverage so the default-template path now asserts `data-section-*` attributes plus breadcrumb markup without requiring a custom template file. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Refreshed the top-level README so it matches the current CLI surface and build pipeline: `onboard` replaces `new`, build now documents frontmatter-aware article/page classification, route-aware WikiLinker rewriting, automatic navigation metadata, and the template variables now exposed to custom or built-in layouts. Documentation-only change.
- 2026-04-12 00:xx +0800 - Extended breadcrumb rendering for article pages without promoting articles into navigation: article templates now render a final current-page breadcrumb label, while section ancestry continues to come only from the existing page-only navigation tree. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Added nested section-article breadcrumb coverage across pretty, direct, and template-file fixtures so article pages under a section now assert `Documentation / Guide Tips` style output while articles still stay out of navigation. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Tightened breadcrumb rendering so standalone articles without a section no longer emit a one-node breadcrumb, while nested section articles still keep inherited `current_section_*` metadata and `Section / Article` breadcrumb output. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Simplified the page-top title bar to show only the current page title, not section ancestry: cli now renders breadcrumb markup as a single current-page label, and docflow/cli coverage was updated across pretty, direct, built-in, and template-file outputs. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Renamed the template context field from `breadcrumb_html` to `page_header_html` so the API matches current behavior now that the page-top slot only renders the current page title. Verified with `moon info && moon fmt && moon test` (90 passed).
- 2026-04-12 00:xx +0800 - Exposed `page_header_title` alongside `page_header_html` in template context so custom templates can consume the current page title as plain text without depending on the built-in HTML fragment shape. Verified with `moon info && moon fmt && moon test` (90 passed).
