- 2026-04-13 00:xx +0800 - Added `data-expanded` to auto-navigation items so current pages and their ancestor chain now expose stable expanded-state markers in both pretty and direct route output. Verified with `moon test` (90 passed).
- 2026-04-13 00:xx +0800 - Taught `moonink serve` to reuse the runtime build pipeline, validate the built preview root through a dedicated preview-runner boundary, and report the preview address/output directory. Added dry-run serve/runtime tests and verified with `moon info && moon fmt && moon test` (95 passed).
- 2026-04-13 14:xx +0800 - Split real mocket preview serving into a standalone `native-serve/` MoonBit subproject, removed `oboard/mocket` from the main workspace, kept the main workspace `serve` path as build-plus-dry-run orchestration only, and let the native entry accept an optional explicit config path for repo-root launches. Verified with `moon info && moon fmt && moon test`, `cd native-serve && moon check --target native && moon build --target native`, and `moon run --manifest-path native-serve/moon.mod.json native-serve/src/cmd/main --target native -- serve fixtures/v2/minimal/moonink.json`.
- 2026-04-13 15:xx +0800 - Updated README and technical CLI/serve docs to distinguish the main-workspace dry-run `serve` contract from the standalone `native-serve/` real mocket preview server, including repo-root launch examples with explicit config paths.
- 2026-04-13 16:xx +0800 - Added the first `moonink check` command slice: CLI dispatch/help coverage, a runtime diagnostics pass that reuses config/discovery/docflow wikilink checks without writing `dist/`, and fixture coverage for clean, warning, and missing-config outcomes.
- 2026-04-13 17:xx +0800 - Added runtime recursive asset copy helpers and taught `moonink build` to copy a project-root `public/` tree into `dist/` before emitting HTML, with fixture coverage for copied text assets, nested paths, binary asset presence, and stale-output cleanup.
- 2026-04-13 18:xx +0800 - Added the first theme MVP slice: build now prefers `theme/layout.html` over `template_file`, copies `theme/assets/` into `dist/assets/`, and exposes `theme_name`, `theme_asset_root`, and `page_body_class` to templates. Verified with `moon info && moon fmt && moon test` (102 passed).
