- 2026-04-13 00:xx +0800 - Added `data-expanded` to auto-navigation items so current pages and their ancestor chain now expose stable expanded-state markers in both pretty and direct route output. Verified with `moon test` (90 passed).
- 2026-04-13 00:xx +0800 - Added `data-current` to auto-navigation items so templates can distinguish current pages from ancestor nodes without re-deriving navigation state in pretty or direct route output. Verified with `moon info && moon fmt && moon check && moon test` (90 passed).
