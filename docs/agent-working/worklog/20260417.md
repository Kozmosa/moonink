01:22 +0800 - Added Theme V2 core config and manifest parsing in `src/core`, including config `theme`/`theme_config` support, minimal manifest/view-model types, and focused core tests for Task 1.
02:15 +0800 - Tightened Task 1 theme config and manifest parsing so `theme_config` preserves JSON-shaped values, malformed manifest entries now fail loudly, and the Task 1 worklog entry was moved to the correct 20260417 log with focused core validation.
03:07 +0800 - Extended core frontmatter and page metadata for Theme V2 page overrides: `layout` now normalizes into `PageMeta.layout_override`, `theme_*` frontmatter is preserved as structured `theme_meta`, minimal theme page view-model exposure landed, and focused core plus affected CLI verification now pass.
03:43 +0800 - Fixed Task 2 review follow-ups so `layout` list-shaped frontmatter now surfaces in check diagnostics, `theme_*` page overrides preserve typed `ThemeConfigValue` scalars and list entries, added focused regression fixtures/tests, and re-ran core plus frontmatter-check verification.
04:17 +0800 - Added the Theme V2 runtime loader for project-local `theme/theme.json` bundles, validating manifest layout files and include references, added focused runtime fixtures/tests for successful bundle loading plus missing partial failures, and kept legacy template/built-in fallback behavior intact for Task 3.
04:49 +0800 - Fixed Task 3 scope drift so legacy active-theme layout loading ignores Theme V2 bundles until later tasks, strengthened runtime coverage for the explicit missing-partial error, and re-ran focused runtime plus narrow CLI verification.
05:08 +0800 - Fixed the remaining Task 3 runtime review issue so Theme V2 bundles with missing or empty `layouts` now fail during bundle loading, added the empty-layout regression fixture/test, and re-ran focused runtime plus minimal broader verification.
06:13 +0800 - Upgraded the docflow template engine for Theme V2 nested path lookup plus minimal `{% if %}`, `{% for %}`, and `{% include %}` support, added focused renderer regression tests, and regenerated docflow interfaces after verification.
