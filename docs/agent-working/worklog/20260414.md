- 2026-04-14 00:xx +0800 - Integrated backlinks inward onto current main by adding markdown wikilink backlink indexing to the build templating path, exposing `backlinks_html` in the template context and built-in theme layout, covering the new pretty-route backlinks output with CLI/docflow/runtime tests, and documenting the milestone in `docs/agent-working/BacklinkImpl0413.md`; verified with `moon check`, `moon info && moon fmt`, and `moon test` (105 passed).
- 2026-04-14 23:56 +0800 - Refreshed `docs/agent-working/MoonInkCliArch.md` so the maintained CLI architecture note now matches the current command surface and runtime behavior: real `build`, non-emitting `check`, delegated/dry-run `serve`, theme/template precedence, navigation/page-header/backlinks template context, and the current known gaps/next steps.
