- 2026-04-14 00:xx +0800 - Integrated backlinks inward onto current main by adding markdown wikilink backlink indexing to the build templating path, exposing `backlinks_html` in the template context and built-in theme layout, and covering the new pretty-route backlinks output with CLI/docflow/runtime tests plus `moon check`, `moon info && moon fmt`, and `moon test` (105 passed).
