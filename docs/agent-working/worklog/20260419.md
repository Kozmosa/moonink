01:24 +0800 - Wrote the Quartz-style product-direction design spec for MoonInk, defining a brand-first article experience plus visible bidirectional-link roadmap with M1/M2/M3 milestone boundaries.
02:35 +0800 - Completed the serve parity slice: main-workspace `moonink serve` now builds once and delegates real preview startup to the native-only backend, added internal prebuilt-preview handoff support plus CLI/native coverage, fixed the missing `site` check diagnostic rendering, and updated the maintained serve architecture/docs.
02:49 +0800 - Implemented Quartz Garden M1 article experience in the main build/theme pipeline: upgraded frontmatter metadata, added heading-anchor/TOC signals, contextual backlinks, related notes, series/prev-next navigation, and refreshed the built-in Theme V2 article surface with fixture-backed validation.
15:46 +0800 - Updated the Quartz-style product-direction spec to reflect completed M1 and reorganized future work into M2 search productization/public shell, M3 frontmatter-driven site behavior, and M4 serve/developer-experience plus exploration milestones.
16:27 +0800 - Clarified in README, CLI/serve technical docs, maintained CLI architecture notes, and the serve implementation note that real `serve` support is intentionally native-only and that the non-native `only available on native targets` message is expected behavior.
18:46 +0800 - Delivered the M2 search/public-shell milestone: productized built-in search with generated search/tag/series/archive surfaces, added homepage curation plus canonical/OG/RSS outputs, expanded Theme V2/default-theme support, and updated the maintained architecture/docs.
18:47 +0800 - Wrote the M3 frontmatter-driven site behavior and collection contract design spec, locking draft/search visibility semantics, dual time-axis ordering, site-led author/profile metadata, and build-owned surface contracts for homepage/search/tags/series/archive.
19:12 +0800 - Implemented Quartz Garden M4 on the main branch baseline: `serve` now builds through a protected staging/publish flow, emits preview status plus full-page live reload assets, runs native watch-mode preview with rebuild-on-change and preview-time error visibility, and the built-in article layout now exposes a stronger local relationship-navigation block backed by structured Theme V2 relationship collections.
19:39 +0800 - Implemented M3 in the `feat/M3` worktree: added typed frontmatter behavior fields plus typed site author/profile config, moved homepage/search/tags/series/archive ordering and visibility into build-owned shared card/collection assembly, generated system pages for search/tag/series/archive, updated the built-in Theme V2 surfaces and search artifact contract, expanded CLI/core coverage, and refreshed the maintained architecture/technical docs.
20:00 +0800 - Post-review fixes: extracted magic numbers to named constants (`serve_watch_max_diagnostic_lines`, `serve_watch_max_mention_entries`, `max_mention_entries_per_page`); fixed Int32 overflow in `hash_bytes` by promoting to Int64; changed `preview_process_still_running_result` return type to `Result[Unit, String]` eliminating dead `Ok(false)` arm and corrected misleading "preview startup" error message in watch loop.
20:58 +0800 - Post-review fixes: replaced O(n²) string concatenation in `escape_html_text` and `render_navigation_items` with `StringBuilder`; validated ISO 8601 format in `archive_group_key` to reject non-padded month/day dates; excluded featured pages from `homepage_recent` to prevent duplicate cards; unified `card_density_label`/`surface_card_density_class` into a single function; fixed always-truthy `{% if collections.series %}` template guard; improved distinct error messages in `parse_author_profile_link`; removed redundant `catch { err => raise err }` no-ops in `config.mbt`.
21:33 +0800 - Wrote two single-binary release design specs: one defining detached native-Linux runtime behavior for onboard/build/check/serve, and one defining the self-contained build pipeline that embeds built-in theme assets plus native preview serving into the main moonink binary.
22:51 +0800 - Wrote a standalone docs/moonink user-documentation vault spec covering audience, shipped-only scope, page inventory, writing rules, showcase requirements, internal source-rewrite map, and acceptance criteria, while recording blockers around undiscoverable binary install paths and unstable frontmatter fields.
23:10 +0800 - Updated the docs/moonink vault spec with the confirmed GitHub Releases distribution source for binary install at github.com/Kozmosa/moonink and converted install-page planning from placeholder status to a release-backed documentation path.
23:40 +0800 - Authored the standalone docs/moonink user-documentation vault on branch docs/moonink-docs-v1: added the full page inventory plus demo assets/public files, validated source-run check/build success, kept the site on the built-in default theme, and constrained content to shipped-only beginner-facing behavior with Theme V2 as the recommended customization path.

01:08 +0800 - added the fixture-based moonink-nemophila Theme V2 shell with Nemophila-inspired index/page/article layouts, assets, and CLI coverage
