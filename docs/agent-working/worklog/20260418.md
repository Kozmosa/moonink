02:12 +0800 - Completed the Theme System V2 fallback pass: fixed built-in bundle loading for absolute workspace paths, verified built-in Theme V2 fallback in focused runtime and CLI tests, and updated the maintained architecture plus implementation docs for PR-ready finalization.
09:06 +0800 - Wired `moonink.json.theme` into Theme V2 and legacy theme resolution, taught root-level content discovery to skip the configured theme directory, added focused runtime/CLI coverage for configured theme selection, and re-ran full moon check/test/info/fmt verification.
14:36 +0800 - Closed the cleanup review findings with TDD: `check` now reports invalid configured theme paths as theme/template errors, nested theme roots only skip the actual theme subtree during discovery, and focused CLI/runtime regressions pass.
14:56 +0800 - Fixed the final PR-readiness blocker so helper filtering now respects `content_dir`-relative theme/template paths, added regressions for configured-content theme placement in runtime and CLI checks, and re-ran focused plus full verification.
17:22 +0800 - Completed the remaining `check-diagnostics-design` contract drift: renamed warning-only document output to `wikilinks`, rendered grouped check diagnostics in fixed spec order with explicit pass/fail wording, added the `check_group_order` regression fixture plus CLI coverage, and updated the maintained implementation and CLI architecture docs.
18:15 +0800 - Added Obsidian-direct output support: expanded vault-friendly defaults, discovered and copied vault assets, inferred README/homepage metadata and titles, supported resource wikilinks, and verified build/check behavior for frontmatter-free vault notes.
18:53 +0800 - Closed the Obsidian direct-output review follow-ups: explicit empty excludes now remain authoritative, inferred home/index notes render as page/index content without frontmatter, and full moon test/check/info/fmt verification passed again.