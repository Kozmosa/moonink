00:24 +0800 - Completed the single-binary runtime-compatibility slice on `feat/binary-compatibility`, keeping `--config` as a host path while preserving project-relative policy paths, embedding the built-in theme/runtime assets, moving native preview serving into the main binary, removing delegated `native-serve/` runtime dependencies, propagating main-entry exit codes, and updating architecture/theme/CLI docs plus runtime/native serve coverage.
01:19 +0800 - Fast-forwarded local `main` to `origin/main`, updated `scripts/publish-moonink-docs.sh` for the single-binary native entry plus embedded-theme sync and robust Moon command failure detection, and fixed the builtin-theme bundle generator/runtime wrapper mismatch so docs `check`/`build` and publish dry-run succeed again on the latest main branch.
01:31 +0800 - Fixed GitHub Pages project-site publishing for `docs/moonink`: set the docs vault `site_url` to `https://kozmosa.github.io/moonink`, taught `scripts/publish-moonink-docs.sh` to rewrite root-relative HTML/search-index URLs plus inject a project-site favicon link during gh-pages publication, and published the corrected site to `origin/gh-pages`.
01:43 +0800 - Added a GitHub Actions workflow to publish `docs/moonink` automatically on pushes to `main` by bootstrapping MoonBit in CI and reusing `scripts/publish-moonink-docs.sh`, and recorded the deployment automation design in a dedicated implementation note.
02:25 +0800 - Replaced the placeholder `docs/moonink/assets/vault-diagram.png` with a real Mermaid-rendered docs-vault diagram, added the checked-in Mermaid source, and verified the docs vault still builds with the rendered asset copied into output.
02:44 +0800 - Rewrote README.md as a polished MoonInk landing page, added README-zh.md, and linked both files to the official docs and live demo at https://kozmosa.github.io/moonink/.
