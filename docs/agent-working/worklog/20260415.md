00:01 +0800 - Added direct-route CLI backlinks coverage in `src/cli/cli_test.mbt`, verifying `wikilinks_direct` output renders backlink markup and `/hello.html` backlinks for the linked HTML destination; verified with `moon test src/cli` and `moon check`.
00:03 +0800 - Added `fixtures/v2/public_assets_basic/dist/` and `fixtures/v2/theme_layout_basic/dist/` to `.gitignore` so generated fixture output stays out of git status, then created a follow-up chore commit.
01:00 +0800 - Added a structured CLI request/flag slice for `build`, `check`, and `serve`, wiring `--config` / `--output` across runtime execution plus `--host` / `--port` for serve and native-serve, adding parser/runtime coverage in `src/cli/cli_test.mbt`, and exporting `run_serve_command_runtime` to remove the remaining unused warning; verified with `moon test src/cli`, `moon check`, and `moon info && moon fmt`.
